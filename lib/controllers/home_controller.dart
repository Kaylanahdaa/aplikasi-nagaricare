// Controller for managing any state (add reactive state if necessary)
import 'package:get/get.dart';

class HomePageController extends GetxController {
  // You can add reactive variables here if needed, for example:
  // var topics = [].obs;
  // var posts = [].obs;
}
